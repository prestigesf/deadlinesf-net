# GoldTrac

This repo does not mint. prestige-mcp mints.

```
cd ../prestige-mcp
npx tsx src/goldtrac-cli-mint.ts --app deadlinesf --payload event.json --spine
```

Nine fields. ML-DSA-65. SOFTWARE. Same kid.
